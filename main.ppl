import core

enum Option<T> {
    None,
    Some(T)
}
/*
let b = Option.Some(10.3);


match(b) {
    Option.None -> println("None"),
    Option.Some(c) -> {
        println(c)
    }
}
if const(Option.Some(x) = b) {
    println(x)
}
*/

fn next()->Option<Int64>{
    return Option.None
}