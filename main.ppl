import core


enum Option<T> {
    None,
    Some(T)
}


fn next()->Option<Int64>{
    return Option.None
}
fn Option<T>.is_some(self)->Bool{
    if const(Option.None = self){
        return false
    }
    return true
}
let a = next()
let b = a.is_some()
if(b){
    println("Is some")
}