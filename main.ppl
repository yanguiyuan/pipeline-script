import core
fn Pointer<T>.create(n: Int64) -> Pointer<T> {

}
struct Vec<T> {
    data: Pointer<T>,
    len: Int64,
    cap: Int64,
}
fn Vec<T>.new() -> Vec<T> {
    const n = 10
    return Vec<T>{
        data: Pointer<T>.create(n),
        len: 0,
        cap: n,
    }
}
let v = Vec<Int64>.new()
