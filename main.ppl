import core

enum Option<T> {
    None,
    Some(T)
}
let b = Option.Some(10.3);

/*
match(b) {
    Option.None -> println("None"),
    Option.Some(c) -> {
        println(c)
    }
}
*/
if let(Option.Some(x) = b) {
    println(x)
}
